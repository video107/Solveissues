$("#like-<%= @issue_id %>").prepend("<%= link_to '支持', unsupport_path(:issue_id => @issue_id), :id => "support-#{@issue_id}" , :remote => true %>");
// $("#like-<%= @issue_id %>").attr('text':'取消支持','method':'create');
// $("#like-<%= @issue_id %>").remove();

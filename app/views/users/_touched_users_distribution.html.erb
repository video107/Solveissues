<div class="col-xs-12 sky-header"><h2>接觸的總人數: <%= touched_user_ids.size %></h2></div>
<h1></h1>

<h3 class="text-center">地區分佈</h3>
<% country_hash = Hash.new %>
<% country_hash = return_total_user_country_hash(User.where(:id => touched_user_ids)) %>
<% country_hash.each_key do |k| %>
  <div class="col-xs-3">
    <h5 class="text-center"><%= k %> : <%= country_hash[k] %></h5>
  </div>
  <div class="col-xs-9">
    <div class="progress">
    <%= p = (country_hash[k]*100/ touched_user_ids.size ).to_i.to_s + "%" %>
      <div class="progress-bar progress-bar-success" role="progressbar" aria-valuenow="40" aria-valuemin="0" aria-valuemax="100" style="width: <%= p %>">
        <span class="sr-only">40% Complete (success)</span>
      </div>
    </div>
  </div>
<% end %>
